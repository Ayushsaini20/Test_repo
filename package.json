{
  "name": "my-repo",
  "version": "1.0.0",
  "scripts": {
    "test": "jest",
    "build": "mkdir -p dist && echo 'Build output' > dist/output.txt"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
